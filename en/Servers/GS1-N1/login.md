# Login
## Main Core-3588SJD4-AI
### HDMI
When logging in via the desktop, it automatically logs in as the user `firefly`, with the password also being `firefly`.

## Main Core-3576JD4
### HDMI
When logging in via the desktop, it automatically logs in as the user `firefly`, with the password also being `firefly`.

## Main Jetson Orin Nano/Jetson Orin NX
### HDMI
When logging in via the desktop, it automatically logs in as the user `nvidia`, with the password also being `nvidia`.